# Readiness Packet: Two CloudTrail Actions, One Approval Gap

This packet exercises action-level readiness. Both CloudTrail side effects share
the same authorization policy and explicit non-revocation export. One action has
a matching authorization decision ID carried into the tool call; the other action
is missing that binding.
