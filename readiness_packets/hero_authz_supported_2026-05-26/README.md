# Hero Authorization Demo: Supported Receipt Artifacts

This packet contains compiler-shaped verifier artifacts for the same action used
by the hero before/after scanner packets:

```text
hero-authz-charge-001
```

It is used by `demo_30s.sh` to close the demo with a bounded receipt card.
